# EstateVision.md — VIABILITY-AXIS REVIEW

**Review object:** dev-docs/EstateVision.md, DRAFT, pinned digest be049338… (attested per protocol), at glade-wz HEAD 5e9114d
**Baseline:** glade-wz root 5e9114d; members glade 71509c1, glial 0dfe4b9, taut 7a5f616, glade-decl bbce73d, glade-decl-ts 7e16e32, grazel 643cc2a; gryth-ui bffbdd0 (last commit 2026-07-12); gwz-core 2214eac; liquid-db c845d76; datascad — no git history ("No commits yet", verified); bizscad unversioned (companion read from disk). All sources read via Read/grep and read-only `git log/status`; no builds, no writes.
**Date:** 2026-08-30
**Axis:** Viability — whether the program the text commits to can be executed and sustained. Independent, adversarial, read-only. The other axis runs in parallel; nothing here relies on it. Filed verbatim by the lane owner.

**Verdict: NO-GO** — 0 P0, 1 P1, 5 P2, 1 P3. Every blocking finding has a bounded, text-fixable remedy. **I pre-commit to GO on a revision that resolves P1-1 and P2-1 through P2-5 as specified.**

---

## 0. Evidence base

**pinned digest: MATCH at start and end.**

- **Object:** /Users/owebeeone/limbo/glade-wz/dev-docs/EstateVision.md (229 lines, read in full). Cited by line below.
- **Companion (ground truth, cited not re-derived):** /Users/owebeeone/limbo/bizscad/dev-docs/EstateReview-2026-08-30.md — esp. §1.3 (binding constraint = ratification throughput), §4 (nine parked decision queues, oldest 2026-07-02), §5 (unversioned value), §2 (no ga020; BlindSide2 status).
- **Architecture:** /Users/owebeeone/limbo/glade-wz/grazel/src/main.rs:124–130 (wire-attachment composition P00-a: one hardcoded supplier spawned at grazel startup; chat surfaces "pre-declared in grazel-app.glade"); grazel/src/lib.rs (1,136 LOC total incl. tests — "prototype" grade confirmed); /Users/owebeeone/limbo/gryth-wz/gryth-ui/src/bootstrap.tsx:6–9 ("plugins self-register on module init (composition-root glob)" — build-time UI composition); /Users/owebeeone/limbo/glade-wz/dev-docs/glade/GladeDeclSurface.md:111–132 (GDL-037: declarations are runtime records; **key TYPES ride taut codegen** — compile-time); /Users/owebeeone/limbo/glade-wz/dev-docs/SupplierOutlines.md:11–16, 191; /Users/owebeeone/limbo/glade-wz/dev-docs/GladeProgramStatus.md:35, 41–43 (P2 enforcement gated on WD-1/AZ-1/2/3); /Users/owebeeone/limbo/glade-wz/CLAUDE.md (GDL-037 re-scope).
- **Velocity/attention (git, since 2026-07-01):** gwz-core 222 commits (last 2026-08-30); glade 30; glial 15; taut 15; grazel 3; gryth-ui 3 — last 2026-07-12 (bffbdd0, unmerged since); wyred last 2026-07-13; liquid-db 3 (gwz enrollment only). datascad: zero commits ever.
- **Dates:** sledger dev-docs last modified Jul 11–12 (ls -la; matches "awaiting approval since 07-12"); BlindSide2-Triage.md thread 3: EE recruit by 08-21, sessions by 08-28, verbatims by 08-31 — companion confirms blown; wyred kill recorded 2026-08-05.
- **Composition exhibit:** six-repo atomic CRDT commit verified — identical message/timestamp 2026-08-29 22:38:35 across root, glade, glial, taut, glade-decl, glade-decl-ts.
- **Grep attestations on the object:** no occurrence of any commodity-stack comparator, baseline, or build-vs-buy question; no occurrence of deployment/ops, backup, data-migration, support, monitoring, privacy/compliance terms (hosting appears once, line 128, as an ABSENT item); no occurrence of any ratification-throughput mechanism (cadence, batching, delegation, deadlines, defaults).

## 1. Findings

### [P1-1] The vision adds decision load to a saturated ratification channel and commits to no throughput mechanism, so no acceptance test is reachable in bounded time as written
- **Location:** §7 (lines 208–219), §2 acceptance tests "for ratification" (line 62), V-7 (line 193), §8 (line 227).
- **Violated invariant:** a program-level commitment must close its own resource math — the resources it names, operated as demonstrated, must suffice to reach its own gates.
- **Evidence:** The program's binding constraint is operator ratification, not build capacity (companion §1.3, §4: nine queues parked on the operator, oldest 2026-07-02). The vision's own claim table concedes V-7 is "contradicted elsewhere by the ratification backlog and unversioned flagships" (line 193). Every acceptance test is downstream of at least two decisions of exactly the stalled class: T1 ← §7.1 + §7.2 + §7.4; T2 ← §7.3 + §7.4 + §7.5; T3 ← T2 + §7.5; T4 ← §7.2 (+ the garns Δ re-measure, itself in the M5 queue parked since 07-20). §7 is largely a **re-listing of decisions already parked 5–8 weeks** (§7.2 ⊇ sledger's 3 questions, 07-12; §7.4 = WD-1/AZ-1..3; §7.5 = the bffbdd0 merge, 07-12) — and the record shows crisply collated decisions still aged 7 weeks, so collation alone is demonstrably insufficient. The vision then adds new load: §7.1, §7.3, ratifying the five §2 tests (line 62), and the V-8 framing decision (line 194). Demonstrated attention is serial: August went to gwz (222 commits, R2-E accepted) while grazel took 3 commits and gryth-ui none since 07-12 — yet the flywheel (§3) requires all five lanes to advance, and its T4→T1→T2→T3 ordering makes the tests serial through the same single operator. The document proposes no cadence, batching, delegation, deadline, or default-on-timeout anywhere (grep attested); §8 instead delegates "sustainability of the one-operator model" to its reviewers as a question.
- **Impact:** Pursued as written, the program's gates are unreachable in bounded time: each is gated on a decision class with demonstrated 5–8-week latency and rising queue depth, while finished work (wyred gate-green, bffbdd0, garns/sledger — garns still in no repo at all) accumulates unharvested. The central viability claim (one person + AI lanes can run this program) is invalidated by the document's own cited evidence, and the document knowingly proceeds without a remedy.
- **Required correction:** Add a §7.0 operating-model decision as the first forcing decision: a committed ratification mechanism (e.g. a recurring ratification sitting working the pre-collated companion-§4 + §7 queue; explicit decision deadlines with named defaults on timeout; delegation rules for sub-vision-level ratifications), and re-grade V-7 to reflect that the mechanism is the compensating control being introduced, not gates alone.
- **Closure test:** the revised text contains a concrete throughput mechanism with dates/defaults, and each §2 acceptance test can name the finite, scheduled decision set it waits on.

### [P2-1] T1's acceptance test cannot discriminate "very fast" — it has no threshold and no comparator, and the document never poses the commodity-baseline question that would supply one
- **Location:** §2 T1 test (lines 63–66); V-4 (line 191); absence attested document-wide.
- **Violated invariant:** an acceptance test must be able to fail the claim it certifies.
- **Evidence:** The test is "one real application is assembled … and the build is *timed*. The claim 'very fast' acquires a number" (lines 63–66). A number is not a pass/fail criterion: any launch, however slow, satisfies the test as written. Nothing anywhere in the object compares assembly on this stack against assembling the same product on a commodity stack (Rails/Django/Supabase class — zero occurrences, grep attested), even though the stack's §4 ABSENT list (enforcement, users, billing, hosting — line 126–129) is precisely the layer commodity stacks provide on day one, and the timing as designed excludes the stack's own multi-year build cost.
- **Impact:** The program's first gate consumes its scarcest resources (operator decisions §7.1/§7.2/§7.4 plus all external-facing build-out) to produce an uninterpretable datum that can neither sustain nor falsify V-4 — and quietly ducks the only economic question that determines whether T1 is the differentiator at all (the object's own §1 endgame, lines 30–35, locates the differentiation in T2's live-extension loop, which no commodity stack offers).
- **Required correction:** Give the T1 test a pre-registered pass criterion — a threshold, or a comparator arm/baseline argument (even a paper one) against a commodity-stack build of the same product — and state the document's answer to why the first product routes through this stack.
- **Closure test:** the revised T1 test states, before the build, what number counts as "very fast" and relative to what.

### [P2-2] T2's acceptance test under-specifies "live": its letter is satisfiable by commodity AI-driven redeploy, while its spirit collides with the estate's own compile-time seams — a constraint the text never surfaces
- **Location:** §2 T2 test (lines 67–70); §4 T2 ABSENT (lines 142–146); §7.3 (lines 214–215).
- **Violated invariant:** an acceptance test must force the mechanism the thrust claims (V-5: extend a *running* app, live), and gap grading must surface architecture-level constraints, not only absences.
- **Evidence:** The test excludes only "a redeploy-by-human" (line 68) — an AI that rebuilds and restarts the app (a commodity CI/CD pipeline plus an LLM) satisfies the letter while exercising none of the hot-join mechanism §4 grades ABSENT ("no mechanism for adding a supplier/surface to a **running** node", lines 142–144). The spirit reading, meanwhile, is harder than §7.3's "needs a design doc and a first vertical slice" suggests: declarations are runtime records by design (GladeDeclSurface.md:119–123 — favorable), but **key TYPES ride taut codegen** (compile-time, per ratified GDL-037; CLAUDE.md), and gryth-ui composes plugins at build time via a module-init glob (bootstrap.tsx:6–9), with grazel spawning its single supplier at startup and chat surfaces "pre-declared in grazel-app.glade" (main.rs:124–130). So any feature introducing new key types or new UI cannot "join live" without a rebuild path or C2 desktop-as-data — C2 having "no implementation" (line 156) behind a branch unmerged 7 weeks. The vision names C2 as T2/T3's critical path (lines 218–219) but never states the compile-time-types constraint or defines what "live" means against it.
- **Impact:** As drafted the test is either passable without evidencing V-5 at all, or silently requires C2 + a typed rebuild-and-rejoin pipeline + enforcement (§7.4) + attribution (stubbed, lines 138–139, 145) — a chain whose size the "design doc and a first vertical slice" framing understates. Planning against this test will either declare victory on a Heroku-class capability or discover the real scope mid-flight.
- **Required correction:** Define "live" in the T2 test (running node retained? which state survives? is AI-driven rebuild admissible, and for which feature classes given codegen'd key types?), and add the compile-time-types / build-time-UI constraint to §4's T2 gap statement.
- **Closure test:** the revised test names the mechanism class that must be exercised and states which of {existing-types-only, new-types-via-rebuild, C2-surfaces-as-data} the first pass may use.

### [P2-3] T4's acceptance test is ambiguous between "passable today by one signature" and "currently unsatisfiable by any live candidate"
- **Location:** §2 T4 test (lines 73–75); §4 T4 (lines 161–172); V-8 (line 194); §7.2 (lines 212–213).
- **Violated invariant:** an acceptance test must be well-posed — its satisfaction conditions must not flip between trivial and unattainable depending on reading.
- **Evidence:** The test conjoins "an experiment reaches the target quadrant (Δ≥3.5, C≥4)" with "at least one experiment graduates into a T1 component" (lines 73–75). Reading A (different experiments may satisfy the conjuncts): the quadrant conjunct is already met by ga019 (Δ=5.0, C=5, line 161), so the whole test reduces to the §7.2 graduation decision — passable by signature, with no new experimental result, and the graduated component (garns Δ=3.0 or sledger Δ=3.3, lines 124–125) would itself have **missed** the quadrant, so the thrust's claim gets "sustained" by a part that failed the program's own bar. Reading B (same experiment must do both, as "a hit that never becomes a part is a paper" implies): no live candidate qualifies — the only hit (ga019/wyred) was ruled a non-graduating research instrument on 2026-08-05 (lines 198–199; BlindSide2-Triage thread 3) and is not a SaaS-stack part; garns/sledger sit below the cutoff; the garns Δ re-measure is parked in the M5 queue since 07-20 and no ga020 exists (companion §2). Additionally the leading graduation candidate, garns (~17k LOC), is in no git repository at all (verified: "No commits yet") — a harvest prerequisite §7.2 omits.
- **Impact:** Under Reading A the test is meaningless; under Reading B it is unscheduled research; the document does not say which, so the thrust cannot be planned or falsified.
- **Required correction:** State the binding reading. If A, require the graduating experiment to independently meet a stated measurement bar; if B, name the path (re-measure garns / extend sledger / ga020) and its prerequisite (version the candidate).
- **Closure test:** a reader can answer "what, exactly, must happen for T4 to pass, and could it pass today?" with one answer.

### [P2-4] The flywheel's compounding-economics claim ("each turn shrinks build time") is load-bearing but missing from the §5 claims table, and current dynamics run the other way
- **Location:** §3 diagram (lines 100–101); §1 (lines 34–35); §5 table (lines 185–194).
- **Violated invariant:** the document's own contract — §5 enumerates the load-bearing claims so §8's review can attack their evidence grades — must cover every claim the vision's payoff depends on.
- **Evidence:** "each turn shrinks build time" (lines 100–101) and "each product built makes the next one faster" (lines 34–35) appear nowhere in §5: V-4 covers one-shot assembly speed and V-6 covers evidence transfer, but no row grades the compounding claim. The demonstrated dynamic is the opposite in the current regime: turns accumulate unabsorbed output rather than speed — wyred parked gate-green since 07-13, bffbdd0 unmerged since 07-12, sledger unapproved since 07-12, garns unversioned — and integration surface grows with each minted part (one demo feature required a six-repo atomic commit, verified; glade-wz alone carries ~19 members, all coherence-kept by one person).
- **Impact:** The vision's central economic promise escapes the exact audit machinery the document builds for itself; planning proceeds on an ungraded premise that the companion's evidence currently contradicts (absorption rate < production rate).
- **Required correction:** Add the compounding claim to §5 as its own row with an honest grade (ABSENT/contradicted-in-current-regime) and its dependency on P1-1's absorption mechanism.
- **Closure test:** §5 contains the row; §3's arrow carries a pointer to it.

### [P2-5] T1's ABSENT list omits launch prerequisites — operations/deployment of the node topology, backup/data migration, support, and privacy/compliance for the named candidate domain
- **Location:** §4 T1 ABSENT (lines 126–129); §7.1 (lines 210–211); §6.2 (lines 200–202).
- **Violated invariant:** the map's stated purpose — measuring the distance from built to acceptance honestly — requires the ABSENT list to be complete for the test it serves ("launched for actual users").
- **Evidence:** The list names enforcement, users/billing/hosting, timing, and the unnamed product — and nothing else. Grep-attested: the object contains no deployment/operations story (who runs the iroh-p2p Rust node plus spawned supplier processes, upgrades, monitoring), no backup/restore or customer-data-migration item, no support/incident model, and no privacy/compliance line — although the standing first-product candidate is **practice management** (line 211; Everbility fixtures live in liquid-db/spike-a — `ledgers/prax_ext.sledger`), a client-records domain where privacy obligations are a launch precondition, and stage-1 security is allow-all (line 128).
- **Impact:** The T1 gap is materially larger than mapped; any "very fast" plan scoped against §4's list will under-scope the launch by the full operations-and-compliance layer, invalidating whatever timing datum P2-1's test produces.
- **Required correction:** Extend §4 T1-ABSENT with the omitted classes (ops/deployment, backup/migration, support, compliance-for-the-candidate-domain) and reflect any that are decision-shaped into §7.
- **Closure test:** the ABSENT list, read alone, is sufficient to scope a real launch of the named candidate.

### [P3-1] T1's test requires a class of action — external contact — the program has never once performed, and §6.3 fences the pressure to develop it onto "its own clock"
- **Location:** §2 T1 "actual users" (line 64); §6.3 (lines 203–204).
- **Violated invariant:** a gate should not depend on a capability with zero demonstrated instances and no forcing function.
- **Evidence:** Every external-contact prescription on the books is untouched with deadlines blown (BlindSide2 thread 3: recruit by 08-21, sessions by 08-28, verbatims due 08-31; companion §2: "(b)–(e) untouched", "no user contact"), while internal artifact production continued in the same window. §6.3 defers external validation indefinitely ("worked on its own clock; T1–T3 ship regardless") — yet T1's own test requires launching to actual users, an external act of larger magnitude than the ones already deferred. §7.1 names a product but contains no user-contact step.
- **Impact:** Concrete consequence: T1's test has an unmodeled precursor (first external user contact) that history says will slip without a forcing function, compounding P1-1's latency.
- **Required correction:** One dated external-contact step attached to §7.1 (e.g. first candidate-user conversation before the build is timed).
- **Closure test:** §7.1 carries a user-facing action with a date.

## 2. Invariant analysis

Attacks that **failed** (evidence the document held):

- **"The stack cannot compose" — failed.** The six-repo atomic CRDT commit is real: identical message and timestamp 2026-08-29 22:38:35 across root, glade, glial, taut, glade-decl, glade-decl-ts (git-verified). The vision's own "n=1 composition exhibit" caveat (V-2, line 188) is honest.
- **"Composition is architecturally static, so T2 is foreclosed" — failed in the strong form.** GDL-037 makes declarations ordinary runtime records ("Loaded, not compiled … the same records dynamic configuration writes", GladeDeclSurface.md:119–123) and suppliers are separate processes attaching over the WS carrier (P00-a, grazel main.rs:124–130) — hot attach is architecturally anticipated, not precluded. The residual, real obstacles (compile-time key types, build-time UI plugins, absent join mechanism) are exactly what P2-2 records; the ABSENT grade itself is honest.
- **"§4's grades are inflated" — largely failed.** Spot-checks corroborate the map: grazel is indeed prototype-thin (1,136 LOC incl. tests, one composed supplier); allow-all/P2 gating confirmed (GladeProgramStatus.md:35, 41–43); C2 has no implementation and bffbdd0 is unmerged since 07-12 (git-verified); gwz's "already met" claim matches the companion (v0.11.1, three channels, R2-E accepted). The document's self-disclosures (V-6 "never examined", V-7's recorded contradiction, T2 "no document names this thrust" candor, §7.5's C2-critical-path admission) are accurate. The gap-size failures found are the specific ones in P2-2/P2-5, not a general pattern of inflation.
- **"The wyred fence is convenient" — failed.** §6.1 matches the recorded 2026-08-05 kill in the BlindSide2 triage (thread 3, verified).

Attacks that **succeeded** are the findings: the resource math does not close (P1-1); three of the five acceptance tests cannot do their job as worded (P2-1, P2-2, P2-3); the flywheel's economic premise evades the document's own claims ledger (P2-4); and the T1 map is incomplete for a real launch (P2-5).

## 3. Risks and next action

**Residual risks below the finding bar:** V-2's n=1 composition evidence means the "layers compose without bespoke glue" premise could still fail at the second, dissimilar feature; the serial flywheel ordering concentrates schedule risk in whichever thrust the operator's attention currently occupies (August: gwz, 222 commits vs 3 in grazel); the unversioned-evidence exposure (garns, bizscad — including the companion review this vision cites as ground truth) means the vision's own evidentiary basis is one disk failure from unreconstructable, though that is recorded ground-level in the companion.

**Single next action this verdict implies:** before (or as part of) ratifying this vision, adopt the P1-1 operating-model mechanism and spend the first scheduled ratification sitting on the pre-collated queue (companion §4 top-to-bottom plus §7.1–§7.5) — that one act simultaneously unblocks the oldest parked decisions, tests the mechanism the revised V-7 would claim, and makes the revised acceptance tests schedulable. The findings are all text-fixable; per the pre-commit above, a revision resolving P1-1 and P2-1..P2-5 as specified earns GO on this axis.
