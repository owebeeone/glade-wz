# Glade decision index: what every box in the decision graph is asking

Generated by `scripts/emit_decision_index.py` from `examples/glade-decisions.gyld.py`, stream `base`, lineage `glade-decision-graph` revision `v1`, snapshot digest `94552adafc38d2c6586193d55808901069cc53d72fdd018ebbd6da180682d52c`. Regenerate it rather than editing it.

24 questions, 45 alternatives and 5 triggers. 4 questions are answerable now; 5 wait on a need or event that has not happened.

What this document is not:

- This is a reading aid over the declaration, not a ruling, a schedule or a plan.
- Statuses, leans and directions are the declaration's own; nothing here selects an alternative.
- The matrix rows, the requirement text, the evidence and the costs stay in GladeBuyBuildMatrix.md; only the row id travels here.
- A docstring this index marks as thin is left exactly as written.

## How to read the graph

**A box is one question**: one decision the owner holds, or one already ruled and kept as an anchor. The box carries the identifier this index is keyed by, its matrix row in brackets, the class name that declares it, and then one line per alternative it offers. Listing an alternative selects nothing.

**The fill colour is the status, and nothing else.**

- A **pale yellow** box (#fef3c7) is `Open`: nobody has decided this and no recommendation is recorded. There may still be alternatives listed; none of them is being put forward.
- A **pale green** box (#dcfce7) is `Lean`: a recommendation is recorded with the evidence it rests on, and you have not ratified it. The starred alternative in the box is that lean, and a lean selects nothing.
- A **pale blue** box (#dbeafe) is `Directed`: you gave direction and it is recorded. Ratification, publication or a witness is still pending, so the question is not closed.
- A **pale grey** box (#e2e8f0) is `Decided`: ruled. It is kept in the graph as an anchor the open questions hang from, so a change to it is visibly a change to everything below it.
- A **near-white** box (#f8fafc) is not a status: a near-white folder is not a question at all. It is a trigger: an external need or event that must happen before the question it gates is worth answering.

**The three arrows.**

- A **solid dark blue** arrow (#1f4e79) is `Requires`: drawn from the prerequisite to the question that waits on it. The question at the head cannot be answered until every question at a tail is decided.
- A **dashed brown-orange** arrow (#b45309) is `Implies`: drawn from a question to another question that one of its alternatives would make live, and labelled with that branch. It is a consequence, not an order: the question at the head only becomes real work if that alternative is taken.
- A **dotted grey** arrow (#6b7280) is `GatedBy`: drawn from a trigger to the question it holds. Answering earlier is speculation rather than a decision.

**A star beside an alternative is the recorded lean or owner direction.** It repeats the evidence; it never means the question has been answered. In a stream that has ruled, the alternative a ruling took reads `(selected)` instead.

**"Answerable now" means all three of these at once**: the question is not already decided, every question it `Requires` is decided, no trigger gates it, and no unchosen alternative elsewhere is what would make it live. That is the capture host's own `roots()` answer; 4 of 24 questions meet it in this stream.

**The tiers** order the table below.

- `anchors`: already decided; the fixed points the rest of the graph hangs from
- `roots`: answerable now: nothing open is above them
- `second`: one open question above them
- `third`: two open questions above them
- `gated`: waiting on a need or event, whatever their prerequisites say
- `branch-induced`: live only if a particular alternative is taken somewhere else

**A thin docstring is marked, never rewritten.** Two mechanical marks are used:

- `short`: fewer than 6 words
- `names-a-subject`: opens with neither a question word nor an instruction, so it names the subject area rather than saying what is being decided

13 records carry one; they are listed at the end. A mark says a reader cannot learn the decision from that sentence alone, not that the decision is wrong.

## Every question, by tier

| Tier | Identifier | Matrix row | The question in plain words | Status | Answerable now, or what blocks it |
|---|---|---|---|---|---|
| anchors | discovery_fold | - | Discovery is a fold over signed records; no consensus cell on the read path. | Decided | ruled |
| anchors | grants_as_data | - | Grants and revocations are share records; one pure check(); no JWT inside Glade. | Decided | ruled |
| anchors | iroh_transport | - | Node-to-node transport is iroh; browsers use a WebSocket to a node. | Decided | ruled |
| anchors | shaku_injector | - | Shaku for dependency injection, at NodeAssembly only. | Decided | ruled |
| anchors | shape_catalogue | - | Delivery shapes are the Taut catalogue; text_crdt is a profile over crdt. | Decided | ruled |
| anchors | store_seam | - | RegistryApi over the fold; StoreApi whole-state blob now, SQLite an engine later. | Decided | ruled |
| roots | key_custody | Q11 | Root, device, node and operator key custody and recovery posture. | Open | **yes** |
| roots | lifecycle_composition | Q10 | How async ownership, drain and reverse-order cleanup are composed. | Directed | **yes** |
| roots | scope_model | I-2 | How dissemination scope is bounded: topology, node trust or cryptography. | Lean | **yes** |
| roots | version_pin | Q2 | Bump iroh from the locked 1.0.2 to 1.2.0 and pin 0.x crates individually. | Lean | **yes** |
| second | async_witness | R27 | The Shaku composition witness DI-E01..E04 with a real async port. | Open | waits on `lifecycle_composition` |
| second | proof_family | Q6 | Capability presentation proof encoding. | Lean | waits on `key_custody` |
| second | transport_key_binding | Q3 | Bind the iroh endpoint key to the Glade node principal. | Lean | waits on `key_custody` |
| third | dissemination | Q4 | How records reach trusted nodes beyond the connect-time pull. | Lean | waits on `scope_model`, `transport_key_binding`, `version_pin` |
| third | identity_adapters | Q7 | Which enterprise identity adapters, if any, ship in v1. | Lean | waits on `proof_family` |
| gated | bulk_transfer | Q5 | Verified, resumable bulk content transfer. | Lean | waits on `version_pin`; gated by `first_bulk_supplier` |
| gated | registry_growth | Q12 | Sharded registries, referrals and indexes toward the growth target. | Lean | waits on `dissemination`; gated by `failure_model`, `first_real_route_slice` |
| gated | simulator_tooling | Q13 | Deterministic runtime simulators and dependency-policy tools for adapter tiers. | Lean | gated by `first_real_route_slice` |
| gated | store_engine | Q8 | When the whole-state blob gives way to an engine, and which. | Lean | gated by `store_pressure` |
| gated | text_engine | Q9 | Whether a second text CRDT engine joins the own profile. | Lean | gated by `editing_need` |
| branch-induced | blind_relay_tier | R14 | Per-share payload encryption semantics for untrusted hops. | Open | only live if `cryptography` is taken |
| branch-induced | metadata_exposure | R8 | Which ids, heads and topic identifiers a non-granted trusted node or relay may observe. | Open | only live if `gossip_overlay` or `node_trust` is taken |
| branch-induced | relay_posture | Q1 | Relay and DNS hosting for the first real-route slice. | Lean | waits on `transport_key_binding`, `version_pin`; only live if `topology` is taken |
| branch-induced | traffic_budget_numbers | R5 | Control, interactive and bulk budgets as quantities; per-lane streams or connections. | Open | only live if `gossip_overlay` or `topology` is taken |

## The questions one by one

### discovery_fold

*tier `anchors`, no matrix row, status **Decided**, declared as `DiscoveryFold` in `glade_decisions:GladeDecisions.discovery_fold`.*

Discovery is a fold over signed records; no consensus cell on the read path.

It offers no alternatives: a decided anchor is a fixed point.

**Must be decided first**: nothing.

**`dissemination` is hanging from it.**

**Ruled in**: `WD-8`, `GDL-037`.

### grants_as_data

*tier `anchors`, no matrix row, status **Decided**, declared as `GrantsAsData` in `glade_decisions:GladeDecisions.grants_as_data`.*

Grants and revocations are share records; one pure check(); no JWT inside Glade.

It offers no alternatives: a decided anchor is a fixed point.

**Must be decided first**: nothing.

**`proof_family`, `scope_model` are hanging from it.**

**Ruled in**: `AZ §2-§6`, `B4`, `B5`.

### iroh_transport

*tier `anchors`, no matrix row, status **Decided**, declared as `IrohTransport` in `glade_decisions:GladeDecisions.iroh_transport`.*

Node-to-node transport is iroh; browsers use a WebSocket to a node.

It offers no alternatives: a decided anchor is a fixed point.

**Must be decided first**: nothing.

**`scope_model`, `transport_key_binding`, `version_pin` are hanging from it.**

**Ruled in**: `GQ-2`.

### shaku_injector

*tier `anchors`, no matrix row, status **Decided**, declared as `ShakuInjector` in `glade_decisions:GladeDecisions.shaku_injector`.*

Shaku for dependency injection, at NodeAssembly only.

It offers no alternatives: a decided anchor is a fixed point.

**Must be decided first**: nothing.

**`async_witness` is hanging from it.**

**Ruled in**: `GDL-048`.

### shape_catalogue

*tier `anchors`, no matrix row, status **Decided**, declared as `ShapeCatalogue` in `glade_decisions:GladeDecisions.shape_catalogue`.*

Delivery shapes are the Taut catalogue; text_crdt is a profile over crdt.

It offers no alternatives: a decided anchor is a fixed point.

**Must be decided first**: nothing.

**`text_engine` is hanging from it.**

**Ruled in**: `GDL-041`.

### store_seam

*tier `anchors`, no matrix row, status **Decided**, declared as `StoreSeam` in `glade_decisions:GladeDecisions.store_seam`.*

RegistryApi over the fold; StoreApi whole-state blob now, SQLite an engine later.

It offers no alternatives: a decided anchor is a fixed point.

**Must be decided first**: nothing.

**`store_engine` is hanging from it.**

**Ruled in**: `GDL-036`.

### key_custody

*tier `roots`, matrix row Q11, status **Open**, declared as `KeyCustody` in `glade_decisions:GladeDecisions.key_custody`.*

Root, device, node and operator key custody and recovery posture.

> The docstring above is what the declaration says, and it is marked `names-a-subject`. It is left as written.

**The alternatives it offers.**

- `owner_held_only`: Keys live only on the owner's devices; loss is loss.
- `recovery_keys`: Offline recovery material minted at genesis.
- `social_recovery`: Threshold recovery across trusted principals. [short]

**Must be decided first**: nothing.

**Deciding it is what `proof_family`, `transport_key_binding` are waiting for.**

**Sources**: `WD-1`, `AZ-7`, `SEC-55-D5`.

### lifecycle_composition

*tier `roots`, matrix row Q10, status **Directed**, declared as `LifecycleComposition` in `glade_decisions:GladeDecisions.lifecycle_composition`.*

How async ownership, drain and reverse-order cleanup are composed.

**The alternatives it offers.**

- `dag_runner`: dagx or dagrs as a DAG task runner; no symmetrical teardown.
- `graceful_shutdown_crate`: tokio-graceful-shutdown as the subsystem supervisor; untyped signalling.
- `sdax_rs`: Owner direction 2026-09-12: sdax-rs (sdax, sdax-tokio, sdax-testkit) at the runtime host. **(the recorded lean, starred in the box)**
- `tokio_primitives`: JoinSet waves, CancellationToken, TaskTracker and a LIFO teardown stack in-house; fallback.

**Must be decided first**: nothing.

**Deciding it is what `async_witness` is waiting for.**

**Sources**: `GDL-049`, `R28`, `AR-08`, `A5`.

### scope_model

*tier `roots`, matrix row I-2, status **Lean**, declared as `ScopeModel` in `glade_decisions:GladeDecisions.scope_model`.*

How dissemination scope is bounded: topology, node trust or cryptography.

**The alternatives it offers.**

- `cryptography`: Per-share payload encryption; unauthorised nodes route ciphertext.
  - taking it would make `blind_relay_tier` live.
- `node_trust`: Any accepted node may hold what its operator is granted; policy at serve. **(the recorded lean, starred in the box)**
  - taking it would make `metadata_exposure` live.
- `topology`: A node connects on a share's protocol only if authorised for that share.
  - taking it would make `relay_posture`, `traffic_budget_numbers` live.

**Must be decided first**: `grants_as_data` (Decided), `iroh_transport` (Decided).

**Deciding it is what `dissemination` is waiting for.**

**Sources**: `IrohGladeMapping §5.2`, `GDL-010`, `AZ §7a`, `INV-5`.

### version_pin

*tier `roots`, matrix row Q2, status **Lean**, declared as `VersionPin` in `glade_decisions:GladeDecisions.version_pin`.*

Bump iroh from the locked 1.0.2 to 1.2.0 and pin 0.x crates individually.

**The alternatives it offers.**

- `bump_to_current`: Take 1.2.0 now; the 1.1.0 fixes cover untrusted EndpointAddr input. **(the recorded lean, starred in the box)**
- `stay_on_lock`: Keep 1.0.2 until the first real-route slice.

**Must be decided first**: `iroh_transport` (Decided).

**Deciding it is what `bulk_transfer`, `dissemination`, `relay_posture` are waiting for.**

**Sources**: `IrohReview §1`, `IrohReview §11`.

### async_witness

*tier `second`, matrix row R27, status **Open**, declared as `AsyncWitness` in `glade_decisions:GladeDecisions.async_witness`.*

The Shaku composition witness DI-E01..E04 with a real async port.

> The docstring above is what the declaration says, and it is marked `names-a-subject`. It is left as written.

**The alternatives it offers.**

- `selection_reopened`: The witness fails; injector selection reopens.
- `shaku_confirmed`: The witness passes; Shaku stays confined to assembly.

**Must be decided first**: `lifecycle_composition` (Directed), `shaku_injector` (Decided).

**No other question is waiting on it.**

**Sources**: `DependencyInjectionEvaluation`, `GDL-048`.

### proof_family

*tier `second`, matrix row Q6, status **Lean**, declared as `ProofFamily` in `glade_decisions:GladeDecisions.proof_family`.*

Capability presentation proof encoding.

> The docstring above is what the declaration says, and it is marked `short`, `names-a-subject`. It is left as written.

**The alternatives it offers.**

- `biscuit`: Biscuit tokens; brings a Datalog runtime and a foreign encoding.
- `macaroons`: Macaroons; caveat chains with a foreign encoding.
- `taut_grants`: Custom signed taut grants with Rust/TS/Python corpus parity. **(the recorded lean, starred in the box)**
- `ucan`: UCAN; JWT-shaped encoding. [short]

**Must be decided first**: `grants_as_data` (Decided), `key_custody` (Open).

**Deciding it is what `identity_adapters` is waiting for.**

**Sources**: `SEC-55-D1`, `AZ §2`.

### transport_key_binding

*tier `second`, matrix row Q3, status **Lean**, declared as `TransportKeyBinding` in `glade_decisions:GladeDecisions.transport_key_binding`.*

Bind the iroh endpoint key to the Glade node principal.

**The alternatives it offers.**

- `binding_record`: A NodeTransportBinding record under the node chain; keys rotate independently. **(the recorded lean, starred in the box)**
- `one_key_both_roles`: The iroh EndpointId is the node id; the identity split is relaxed.

**Must be decided first**: `iroh_transport` (Decided), `key_custody` (Open).

**Deciding it is what `dissemination`, `relay_posture` are waiting for.**

**Sources**: `IrohGladeMapping §5.2`, `GladeDirectoryNotes ambiguity 2`.

### dissemination

*tier `third`, matrix row Q4, status **Lean**, declared as `Dissemination` in `glade_decisions:GladeDecisions.dissemination`.*

How records reach trusted nodes beyond the connect-time pull.

**The alternatives it offers.**

- `gossip_overlay`: iroh-gossip topics per share as change notification; pinned 0.x.
  - taking it would make `metadata_exposure`, `traffic_budget_numbers` live.
- `sync_round_only`: The Glade sync round and best-effort push; no overlay. **(the recorded lean, starred in the box)**

**Must be decided first**: `discovery_fold` (Decided), `scope_model` (Lean), `transport_key_binding` (Lean), `version_pin` (Lean).

**Deciding it is what `registry_growth` is waiting for.**

**Sources**: `IrohGladeMapping §4.2`, `INV-D5`, `INV-D6`.

### identity_adapters

*tier `third`, matrix row Q7, status **Lean**, declared as `IdentityAdapters` in `glade_decisions:GladeDecisions.identity_adapters`.*

Which enterprise identity adapters, if any, ship in v1.

**The alternatives it offers.**

- `kerberos_ldap`: Kerberos, AD or LDAP facts through an adapter.
- `none_in_v1`: Device certs and manual grants only; keep the trust-provider interface. **(the recorded lean, starred in the box)**
- `oidc`: OIDC/OAuth2 as edge authentication input. [short]
- `spiffe`: SPIFFE/SPIRE workload identities for nodes and providers.

**Must be decided first**: `proof_family` (Lean).

**No other question is waiting on it.**

**Sources**: `AZ-3`, `SEC-55 trust providers`.

### bulk_transfer

*tier `gated`, matrix row Q5, status **Lean**, declared as `BulkTransfer` in `glade_decisions:GladeDecisions.bulk_transfer`.*

Verified, resumable bulk content transfer.

> The docstring above is what the declaration says, and it is marked `short`, `names-a-subject`. It is left as written.

**The alternatives it offers.**

- `defer_bulk`: No bulk path until a supplier needs one.
- `iroh_blobs_port`: iroh-blobs behind a new bulk-transfer port with an admission wrapper. **(the recorded lean, starred in the box)**
- `own_chunked_streams`: Own chunked framing over QUIC streams; no content addressing.

**Must be decided first**: `version_pin` (Lean).

**No other question is waiting on it.**

**It is gated by**:

- `first_bulk_supplier`: A supplier declares a bulk profile (files, snapshots, large results).

**Sources**: `IrohGladeMapping §4.3`.

### registry_growth

*tier `gated`, matrix row Q12, status **Lean**, declared as `RegistryGrowth` in `glade_decisions:GladeDecisions.registry_growth`.*

Sharded registries, referrals and indexes toward the growth target.

> The docstring above is what the declaration says, and it is marked `names-a-subject`. It is left as written.

**The alternatives it offers.**

- `delegated_referrals`: Authenticated referrals and local preference over the same fold.
- `ordered_reconfiguration`: Consensus-backed shard split, move and membership.
- `trusted_mapping_only`: An explicit trusted namespace-to-shard mapping; nothing dynamic. **(the recorded lean, starred in the box)**

**Must be decided first**: `dissemination` (Lean).

**No other question is waiting on it.**

**It is gated by**:

- `failure_model`: A written registry failure and membership model.
- `first_real_route_slice`: The fixed-peer iroh route slice from GladeBuildEntry passes.

**Sources**: `GAD-04`, `GAD-05`.

### simulator_tooling

*tier `gated`, matrix row Q13, status **Lean**, declared as `SimulatorTooling` in `glade_decisions:GladeDecisions.simulator_tooling`.*

Deterministic runtime simulators and dependency-policy tools for adapter tiers.

> The docstring above is what the declaration says, and it is marked `names-a-subject`. It is left as written.

**The alternatives it offers.**

- `dependency_policy_tools`: cargo-deny and related checks beside the own gate.
- `deterministic_runtime_sim`: madsim or turmoil for adapter-tier journeys.
- `own_sim_only`: glade-discover-sim and the taut corpora only. **(the recorded lean, starred in the box)**

**Must be decided first**: nothing.

**No other question is waiting on it.**

**It is gated by**:

- `first_real_route_slice`: The fixed-peer iroh route slice from GladeBuildEntry passes.

**Sources**: `R31`, `R32`.

### store_engine

*tier `gated`, matrix row Q8, status **Lean**, declared as `StoreEngine` in `glade_decisions:GladeDecisions.store_engine`.*

When the whole-state blob gives way to an engine, and which.

**The alternatives it offers.**

- `redb_engine`: redb behind StoreApi; not evaluated for Glade.
- `sqlite_engine`: SQLite behind StoreApi; never the replication mechanism.
- `whole_state_blob`: Keep the taut blob until size or query volume hurts. **(the recorded lean, starred in the box)**

**Must be decided first**: `store_seam` (Decided).

**No other question is waiting on it.**

**It is gated by**:

- `store_pressure`: Measured size or query volume the whole-state blob cannot carry.

**Sources**: `GDL-036`, `GladePersistenceReview`.

### text_engine

*tier `gated`, matrix row Q9, status **Lean**, declared as `TextEngine` in `glade_decisions:GladeDecisions.text_engine`.*

Whether a second text CRDT engine joins the own profile.

**The alternatives it offers.**

- `automerge`: Automerge 3.x; large browser bundle. [short]
- `loro`: Loro 1.x; Fugue; no Python parity.
- `own_text_profile`: text_crdt.profile/v1 over the own crdt engine; shipped. **(the recorded lean, starred in the box)**
- `yrs`: yrs/Yjs family; documented cross-language wire intent; small browser bundle.

**Must be decided first**: `shape_catalogue` (Decided).

**No other question is waiting on it.**

**It is gated by**:

- `editing_need`: An editing requirement the own text profile cannot meet.

**Sources**: `GDL-041`, `GLResearchConsolidatedFindings`.

### blind_relay_tier

*tier `branch-induced`, matrix row R14, status **Open**, declared as `BlindRelayTier` in `glade_decisions:GladeDecisions.blind_relay_tier`.*

Per-share payload encryption semantics for untrusted hops.

> The docstring above is what the declaration says, and it is marked `names-a-subject`. It is left as written.

No alternatives are recorded for it yet.

**Must be decided first**: nothing.

**No other question is waiting on it.**

**It is only live if** `cryptography` is taken.

**Sources**: `AZ-10`.

### metadata_exposure

*tier `branch-induced`, matrix row R8, status **Open**, declared as `MetadataExposure` in `glade_decisions:GladeDecisions.metadata_exposure`.*

Which ids, heads and topic identifiers a non-granted trusted node or relay may observe.

No alternatives are recorded for it yet.

**Must be decided first**: nothing.

**No other question is waiting on it.**

**It is only live if** `gossip_overlay` or `node_trust` is taken.

**Sources**: `GDL-010`, `WD-3`.

### relay_posture

*tier `branch-induced`, matrix row Q1, status **Lean**, declared as `RelayPosture` in `glade_decisions:GladeDecisions.relay_posture`.*

Relay and DNS hosting for the first real-route slice.

> The docstring above is what the declaration says, and it is marked `names-a-subject`. It is left as written.

**The alternatives it offers.**

- `community_dev_only`: n0 free relays for development only; not for production.
- `iroh_services_pro`: Paid shared relays at 19 USD per month.
- `self_hosted`: Run iroh-relay and iroh-dns-server; free; authenticated relays available. **(the recorded lean, starred in the box)**

**Must be decided first**: `transport_key_binding` (Lean), `version_pin` (Lean).

**No other question is waiting on it.**

**It is only live if** `topology` is taken.

**Sources**: `IrohReview §7`, `GladeDiscoveryModel §5`.

### traffic_budget_numbers

*tier `branch-induced`, matrix row R5, status **Open**, declared as `TrafficBudgetNumbers` in `glade_decisions:GladeDecisions.traffic_budget_numbers`.*

Control, interactive and bulk budgets as quantities; per-lane streams or connections.

> The docstring above is what the declaration says, and it is marked `names-a-subject`. It is left as written.

No alternatives are recorded for it yet.

**Must be decided first**: nothing.

**No other question is waiting on it.**

**It is only live if** `gossip_overlay` or `topology` is taken.

**Sources**: `GPI-03`, `GLA-072`.

## The docstrings that do not explain their own decision

Each line is the declaration's own text, unchanged, beside the marks it earned. Fixing one means editing `examples/glade-decisions.gyld.py` and running this host again.

| Identifier | Kind | Marks | What it says today |
|---|---|---|---|
| async_witness | Question | names-a-subject | The Shaku composition witness DI-E01..E04 with a real async port. |
| automerge | Alternative | short | Automerge 3.x; large browser bundle. |
| blind_relay_tier | Question | names-a-subject | Per-share payload encryption semantics for untrusted hops. |
| bulk_transfer | Question | short, names-a-subject | Verified, resumable bulk content transfer. |
| key_custody | Question | names-a-subject | Root, device, node and operator key custody and recovery posture. |
| oidc | Alternative | short | OIDC/OAuth2 as edge authentication input. |
| proof_family | Question | short, names-a-subject | Capability presentation proof encoding. |
| registry_growth | Question | names-a-subject | Sharded registries, referrals and indexes toward the growth target. |
| relay_posture | Question | names-a-subject | Relay and DNS hosting for the first real-route slice. |
| simulator_tooling | Question | names-a-subject | Deterministic runtime simulators and dependency-policy tools for adapter tiers. |
| social_recovery | Alternative | short | Threshold recovery across trusted principals. |
| traffic_budget_numbers | Question | names-a-subject | Control, interactive and bulk budgets as quantities; per-lane streams or connections. |
| ucan | Alternative | short | UCAN; JWT-shaped encoding. |
