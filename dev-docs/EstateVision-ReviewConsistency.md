# EstateVision.md — CONSISTENCY-AXIS REVIEW

**Review object:** dev-docs/EstateVision.md, DRAFT, sha256 be0493385ef71cc51ba4e6af5005e7b00a96cf5308bc3490b40ba261a6603876, at glade-wz HEAD 5e9114d
**Baseline:** Object hash verified at review start and end (`shasum -a 256`: identical both times; glade-wz HEAD 5e9114dc859f9526cf7b34856d2ab05811c97f3d unchanged; working tree clean except the untracked object). Repos consulted read-only at: gryth-wz 7316086, gryth-wz/gryth-ui bffbdd0 (branch `glp-0006-p1s4-gryth-panels` checked out), glymik-dev d26e660, wyred-wz bf6d8a6, gwz-dev 563d352, elecscad a1a0d76, liquid-db c845d76. bizscad is not a git repo and datascad has no commits (both confirmed by failed `rev-parse`, matching the companion's §5) — their documents were read as working-tree files. Companion EstateReview-2026-08-30.md pinned at sha256 7a924ce312d2843729dee4070ef7b9bc5be2ebf9133dece93f5032f282111eee.
**Date:** 2026-08-30
**Axis:** Consistency — the document against its controlling graph. Independent, adversarial, read-only. The other axis runs in parallel; nothing here relies on it. Filed verbatim by the lane owner.

**Verdict: NO-GO** — 2 × P2, 2 × P3 open; 0 × P0/P1. All four findings are bounded, text-fixable errors in an otherwise verification-clean document. **I pre-commit to GO on a revision that resolves {P2-1, P2-2} as specified** (resolution of P3-1/P3-2 recommended in the same pass).

---

## 0. Evidence base

Object: /Users/owebeeone/limbo/glade-wz/dev-docs/EstateVision.md (229 lines, 12512 bytes; §1 lines 15–43, §2 lines 47–79, §3 lines 82–108, §4 lines 111–180, §5 lines 183–194, §6 lines 196–206, §7 lines 208–219, §8 lines 221–229).

Controlling documents read at source:
- /Users/owebeeone/limbo/glade-wz/CLAUDE.md (north star): contains verbatim "an imperative, stateful mess" describing the GripLab server — object line 39's quote and attribution are exact.
- /Users/owebeeone/limbo/gryth-wz/dev-docs/GrythVision.md lines 38–86: C1 "Everything on screen is a declared surface", C2 "The desktop itself is data: an environ-scoped document…", C3 "Humans and agents are the same kind of participant"; design position 5 "(principal, on_behalf_of)… day one, even while auth itself is stubbed" — object lines 40–42 and 138–140 characterize all four correctly.
- /Users/owebeeone/limbo/bizscad/dev-docs/DeclarativeApiEvaluator.md line 106: "Cutoffs (mechanical, tunable): Δ ≥ 3.5 = high-Δ; C ≥ 4 = high-C", target = high-Δ/high-C, and line 118: only a *measured* card counts — the object's T4 test "(Δ≥3.5, C≥4 measured)" (line 73–74) matches the instrument exactly.
- /Users/owebeeone/limbo/bizscad/dev-docs/BlindSide2-Triage.md: 7 thread rows; thread 3 decision "**ACCEPT** (D3, by exit: wyred demoted from product candidate to research instrument — recorded kill)… recorded 2026-08-05… Compete-with-atopile… stay dead"; threads 1, 2, 4, 5, 6, 7 decision cells "—". Object's "ruling 2026-08-05, BlindSide2 thread 3" (line 199) and "6 of 7 triage decision cells blank" (line 171) verified exactly.
- /Users/owebeeone/limbo/glymik-dev/docs/Unified Platform Vision.md lines 488–502 (Scenario 6 "AI-built combined workflows… Future direction: yes, explicitly") and 598–603 ("AI-assisted creation of new workflows and composed applications") — see P3-2.
- /Users/owebeeone/limbo/bizscad/dev-docs/DeclarativeManifesto.md (thesis paragraph): "Business rules and operational authority should become explicit, typed, inspectable, executable data…" — object line 42–43's characterization is a fair paraphrase, no quote misuse.
- Companion /Users/owebeeone/limbo/bizscad/dev-docs/EstateReview-2026-08-30.md §§1–8 — cross-checked wherever the object repeats a claim.

Estate spot-verifications (command → result):
- `git -C glade-wz show 5e9114d` + marker `gwz.conf/markers/01a04d87-….yaml` + `git diff 0a56741 5e9114d -- gwz.conf/gwz.lock.yml`: commit dated 2026-08-29, "Add collaborative text CRDT demo"; `committed_targets` = glade-decl, glade-decl-ts, glade-decl-rs, glade-decl-py, glade, taut, glial + @root; all seven member pins moved; per-member `git show` confirms substantive content in every one — see P3-1.
- gryth-wz/gryth-ui/src/seam.test.ts lines 11, 42–44: "Headless seam test…", "lets a headless participant open a window on the desktop… through the same ops the chrome uses" — object line 134–135 verified. (Green status of the test relied on the companion; tests were not run, per read-only rules.)
- gryth-wz/gryth-ui/dev-docs/GlialWorkspaceNameCutover.md (2026-08-22): "Only the composition-root provider changed… consumers MUST NOT import Glial" — object line 137–138 verified.
- glade-wz/glade-gwz/src/exec.rs line 31: `ALLOWED_VERBS: &[&str] = &["status", "ls", "diff"]`; src/supplier.rs line 10: "failure-as-DATA" — object lines 135–136 verified.
- glade-wz/taut: tags v0.9.0/v0.9.1; README line 15 "implemented across nine languages" — object line 120 verified. grip-core 0.2.1 / grip-react 0.2.0; grip consumed by glade-wz, gryth-wz (workspace dep), wyred-wz, glial-dev — "npm 0.2.x, four consumer workspaces" (line 120) verified.
- gwz-dev/dev-docs/CurrentProgramCheckpoint.md lines 2501, 2536–2551: "v0.11.1 IS RELEASED (2026-08-29)… THE THIRD CHANNEL: PyPI IS AT 0.11.1… complete across all three channels"; line 2620 "R2-E is done" — object lines 76, 177 verified.
- gryth-ui: `main..bffbdd0` = 1 commit, bffbdd0 dated 2026-07-12, not on main (7 weeks to date) — object line 157 verified (object correctly says only "unmerged"; the branch is now on origin). Test-case count across `*.test.ts*` = exactly 74; TS/TSX LOC in src+packages = 6,973 ≈ "7k" — object line 154 verified. scripts/no-react-state.test.mjs exists — line 151 verified. packages/plugins contains a `gwz` plugin; wyred plugin family lives in wyred-wz — line 149–150's three named consumers exist.
- glade-wz/dev-docs/GladeProgramStatus.md line 35: "P2 enforcement remains gated on WD-1/AZ-1/2/3" — object lines 128, 216–217 verified.
- wyred-wz/dev-docs/WyredPlanIndex.md lines 103–110: "wyred-buildout-phase1 workflow (22 agents, 0 errors)… independent verify PASS (run_gate green end-to-end…)" — object line 162–163's wording matches the estate record verbatim; the ~41k LOC / 7-repo figures rest on the companion's measurement; "independent verify" is an independent *gate run*, still inside the founder+LLM loop — a caveat the object itself carries at lines 169–173 (external validity ABSENT) and V-3.
- elecscad/rl/RESUME.md line 14: "Formal score (Δ=5.0, C=5, oracle 5…) → TARGET quadrant, measured — first candidate in the program to reach it" (boards: Watchy + MPPT per triage thread 3) — object line 161 verified.
- datascad/rl/M5-review.md lines 13–14: "C = 4… Δ = 3.0… below the 3.5 high-Δ cutoff"; M1–M5 review files present — object line 163–164 verified.
- liquid-db/spike-a/dev-docs/Scorecard.md lines 21–28: "Δ = 3.3… C = 5… silent 0% of constructable mutants… safe-tool, grazing the target corner" — object line 164–165 verified.
- sledger-wz/dev-docs/MemberRepoProposal.md lines 17–31: "compiled artifact… CompiledLedger IR… a Postgres backend later consumes the same IR" — §6 fence 2's "sledger's compiled-artifact proposal" (line 202) verified.
- datascad/dev-docs/DatascadPhase0Spec.md: SQLite-only lowering in phase 0, Rust bindings, Glade/Grazel/Gryth as downstream clients — no contradiction with §6 fence 2 or §7.2.
- `grep -rl everbility` across liquid-db, sledger-wz, glade-wz/dev-docs, datascad, bizscad: hits only in sledger's spike/proposal and the object itself — "Everbility appears only as sledger's demo fixture" (line 129) verified.
- glade-wz/dev-docs/glade/suppliers/: exactly ten glade-*.md supplier specs — "ten written specs" (line 142) verified. Members mem_glade_chat and mem_glade_gwz exist — "two instances (chat, gwz)" (line 142) verified.

## 1. Findings

### [P2-1] T1's acceptance test cannot reject any outcome, while §5 V-4 states the bar it omits
- **Location:** §2, lines 63–66 ("one real application is assembled… and the build is *timed*. The claim 'very fast' acquires a number") vs §5 line 191 (V-4: "Assembly from the stack is *fast* — **days, not months**").
- **Violated invariant:** an acceptance test must be able to falsify the thrust it accepts; a test that passes while the thrust as stated fails is defective (review contract), and a document must not state a pass bar in one section and omit it from the section that gates on it.
- **Evidence:** the T1 thrust is "A **very fast** to build and launch SaaS stack" (line 52, operator-verbatim). The drafted test is satisfied by any assembled, launched, timed build — a 14-month build passes it — so "very fast" can be false while the test passes. The same document already commits to a concrete bar, "days, not months," in V-4, but the test does not reference it.
- **Impact:** if ratified as written, T1 can be declared met without the thrust's defining property ever being tested; the vision's own claims table (V-4) and its acceptance gate diverge on what "fast" means.
- **Required correction:** wire the V-4 bar (or an operator-ratified threshold) into the T1 test — e.g. "…and the assemble-to-launch build time is under the ratified bar (V-4: days, not months)" — or explicitly re-title the T1 item a measurement gate and add a separate pass criterion for the thrust.
- **Closure test:** revised §2 T1 test names a numeric or ratified qualitative threshold such that a slow build fails it, and §5 V-4 cites the same bar.

### [P2-2] §7.4's universal — "allow-all cannot meet any acceptance test above" — is contradicted by the document's own test set
- **Location:** §7 item 4, lines 216–217, vs §2 lines 62–79.
- **Violated invariant:** internal coherence — a decision-queue rationale must be true of the sections it quantifies over.
- **Evidence:** of the five §2 acceptance tests, only T2's requires capability gating ("attributed and capability-gated," line 69). The gwz test is declared "already met" (line 76) — met while glade stage-1 allow-all stands, which directly falsifies "cannot meet any." The T4 test (target quadrant + graduation, lines 73–75) is satisfied by experiments (ga019/garns/sledger) that never touch glade's authz at all, and the T3 test (console is a gryth application) has no security dependency as drafted. Verified externally: allow-all is real (GladeProgramStatus.md line 35; glade-gwz exec.rs "stage-1 read-only allow-list"), so the premise is right but the quantifier is wrong.
- **Impact:** §7 exists to order operator decisions; item 4's rationale tells the operator every program gate is blocked on WD-1/AZ-1..3 when, by the document's own tests, three of five are not. A ratified false universal about the doc's own acceptance structure distorts exactly the prioritization §7 is for.
- **Required correction:** narrow the claim to what holds — e.g. "allow-all cannot meet T2's acceptance test, and no T1 launch to actual users is defensible on it" (the T1 half becomes true if P2-1's fix makes real-user launch security-bearing).
- **Closure test:** §7.4's rationale, checked against each §2 test individually, is true of every test it quantifies over.

### [P3-1] The composition exhibit is a seven-member-repo atomic commit, not "six-repo"
- **Location:** §4 T1, lines 121–122 ("one atomic six-repo gwz commit"); repeated at §5 V-2, line 188 ("six-repo CRDT commit").
- **Violated invariant:** exactness of a load-bearing evidence citation — §5's evidence column is the surface the review is invited to attack (§8), so its identifiers must survive verification.
- **Evidence:** commit 5e9114d (2026-08-29, "Add collaborative text CRDT demo"): marker 01a04d87-… lists `committed_targets` = glade-decl, glade-decl-ts, glade-decl-rs, glade-decl-py, glade, taut, glial + @root; the lock diff moves exactly those seven member pins; per-member `git show` confirms every one of the seven carries substantive source/corpus changes (none is marker-only). No accounting yields six. (The companion's own parenthetical "glade + glial + taut + glade-decl×4" already enumerates seven while saying six; the object repeats the wrong number, so the object owns it.)
- **Impact:** an auditor verifying V-2's single composition exhibit against the marker finds the count wrong and the evidence chain loses credibility — needlessly, since the true exhibit (seven member repos plus the workspace root, atomically) is *stronger* than claimed.
- **Required correction:** "seven-repo" (or "seven member repos plus the workspace root") at both sites, lines 121–122 and 188.
- **Closure test:** the stated repo count equals the marker's `committed_targets` member count.

### [P3-2] "No document names this thrust as a goal" overstates against glymik UPV Scenario 6
- **Location:** §4 T2 ABSENT, lines 144–146 ("**and no document names this thrust as a goal** — it exists in fragments (… glymik's HybridApp vision). This document is the first place it is stated directly."); premise reused at §7.3, line 214 ("this document is its first naming").
- **Violated invariant:** claims about the controlling corpus must hold at the cited sources.
- **Evidence:** Unified Platform Vision.md Scenario 6 (lines 488–502): "AI-built combined workflows — Agents can create or extend HybridApps, combine services, and establish new workflow graphs dynamically… Future direction: **yes, explicitly**"; reinforced at lines 598–603 ("AI-assisted creation of new workflows and composed applications"). A document does name (the build-and-join core of) this thrust as an explicit goal. The absolutist sentence survives only under the full-thrust reading (view-internals + command + live launch + attribution on glade/glial/gryth stated as a program thrust — which glymik indeed does not do; its statement is a beyond-MVP scenario entry in a superseded-layer doc), and the object's own parenthetical concedes the fragment.
- **Impact:** concrete and bounded — §7.3 charters T2 from a "first naming" premise; a charter written on that premise can skip reconciling with glymik's prior art (scenario ladder, MLP/MVP staging, HeadlessSession machinery) that the estate already holds for exactly this loop.
- **Required correction:** qualify — e.g. "no document states the full thrust as a program goal (glymik's Scenario 6 names the build-and-join half, explicitly, as a beyond-MVP future)" — and have §7.3's charter item cite Scenario 6 as prior art to reconcile or supersede.
- **Closure test:** the revised sentence is true against a direct read of UPV Scenario 6, and §7.3 acknowledges the predecessor statement.

## 2. Invariant analysis

Attacks mounted and their outcomes; failed attacks are results.

- **Verbatim quotes and characterizations at source.** "an imperative, stateful mess" — exact, correctly attributed to the glade-wz CLAUDE.md north star. C1/C2/C3 — all three match GrythVision.md's commitment headings and body; "(principal, on_behalf_of)… designed and stubbed" matches design position 5. The (Δ≥3.5, C≥4, measured) parenthetical in the T4 test reproduces the Evaluator's cutoff line and its measured-only rule exactly — this attack failed cleanly. HELD.
- **§6 fences against the estate's record.** The 2026-08-05 ruling is real, correctly dated, correctly located (thread 3), and correctly characterized (demotion to research instrument; compete-with-atopile dead). Fence 2's "compiled-artifact proposal" exists verbatim in sledger's MemberRepoProposal ("CompiledLedger IR… a Postgres backend later consumes the same IR"); the garns Phase-0 spec is SQLite-only, consistent. Fence 4's parked gvid/krona matches the estate. HELD (residual risk on the stale pre-ruling forward plan, below).
- **§4 map factual claims.** Fourteen load-bearing facts spot-verified directly against the estate (taut 0.9/nine languages, grip 0.2.x/four workspaces, seam test, cutover doc, glade-gwz verbs + failure-as-data, ten supplier specs + two instances, gwz v0.11.1/three channels/R2-E, bffbdd0 unmerged 7 weeks, 74 tests/≈7k LOC, no-React-state scanner, WD-1/AZ-1..3 gate, ga019/garns/sledger scores at their scorecards, 22-agents/0-errors/independent-verify record, six-of-seven blank cells, Everbility-only-as-fixture). Thirteen verified exactly; one (the commit-count) failed → P3-1. The wyred buildout's PROVEN grade is consistent with the object's own grading key (a recorded gate measurement exists) and its inside-the-loop provenance is properly bounded by the same section's external-validity ABSENT bullet and V-3 — the provenance attack did not convert into a finding.
- **§1↔§3↔§4↔§5 coherence.** §3's load-bearing transfer property is explicitly surrendered to V-6 ("Asserted here; never examined") — honest, consistent. V-1/V-2/V-4/V-5/V-7 evidence grades match their §4 counterparts (demo-grade vs enforcement, n=1 exhibit, never-timed, no join mechanism, gwz-proven/backlog-contradicted with correct EstateReview §4–§5 pointers). §1's aspirational present tense is fenced by §4's ABSENT rows and §8's own charter. HELD except where §7.4 quantifies over §2 (P2-2).
- **§2 tests as falsifiers.** T2's test genuinely falsifies the build-and-join-live half and correctly defers the view/operate half to §4's weaker, existing test; T3's test is satisfiable and on-point; T4's test matches the instrument and is falsifiable via its graduation conjunct (its quadrant conjunct being already met by ga019 is disclosed in §4); gwz's "already met" is verified. T1's test failed the falsifier attack → P2-1.
- **Unstated impacts.** GLP-0006 status, DatascadPhase0Spec, and the gwz checkpoint contain nothing contradicting the vision, its fences, or §7. The one live contradiction in the estate — wyred-wz/dev-docs/WyredForwardPlan.md (actually the *ElecScad* forward plan, mtime Jul 11, pre-ruling) whose Phase 2 "Reconcile & productize" was never reconciled with the 2026-08-05 kill — is already disclosed in the companion the object designates for ground facts (§2 and §4), and the triage ledger is the controlling outcome record by its own rule ("nothing else counts as an outcome"), so the fence stands; no finding.

## 3. Risks and next action

Residual risks below the finding bar: (1) the stale pre-ruling ElecScad forward plan (Phase 2 "productize") remains on disk unreconciled with the thread-3 kill that §6 cites — the companion's recommended resolution should ride along with any §6 ratification; (2) "passing seam test" and CI-green claims were verified to the artifact level only (tests exist and are enumerated; nothing was executed under read-only rules) and otherwise rest on the companion, whose facts self-declare as working-tree-verified on 2026-08-30; (3) "three **external** consumers" (§4 T3) counts two consumers whose UI packages live inside the gryth-ui repo (the gwz panel, the glade tabs) — accurate for the property claimed (outside tool families adopting the shell) but a pedantic reader may stumble; (4) the wyred ~41k-LOC / 7-repo figures are the companion's measurements, not re-derived here.

Next action implied by this verdict: a single revision pass on the object fixing P2-1 (thread V-4's bar into the T1 test), P2-2 (narrow §7.4's quantifier), P3-1 ("seven-repo", both sites), and P3-2 (qualify the first-naming claim and cite glymik Scenario 6 in §7.3) — then re-review is mechanical against the closure tests above. I pre-commit to GO on a revision that resolves {P2-1, P2-2} as specified.
